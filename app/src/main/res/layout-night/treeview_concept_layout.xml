<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/treeview_constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:layout_margin="10dp">

    <ImageView
        android:id="@+id/note_image"
        android:layout_width="31dp"
        android:layout_height="31dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/concept_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tree_concept_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/note_image"
            android:layout_centerHorizontal="true"
            android:layout_marginLeft="15dp"
            android:gravity="center"
            android:text="2 Guerra Mondiale"
            android:textSize="16dp"
            android:textColor="@color/white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/note_image" />

</androidx.constraintlayout.widget.ConstraintLayout>